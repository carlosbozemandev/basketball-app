// Defining a set of colors for consistent theming across the application
const colors = {
  primary: "#f4511e", // Primary color used for key elements
  secondary: "#FF3B30", // Secondary color for alternative elements
  background: "#F2F2F7", // Background color for overall app background
  text: "#1C1C1E", // Text color for general text elements
  border: "#C7C7CC", // Border color for separating elements
};

// Defining a set of sizes for consistent styling across the application
const sizes = {
  small: 12, // Small text or element size
  medium: 16, // Medium text or element size (default)
  large: 20, // Large text or element size
  extraLarge: 24, // Extra-large text or element size
};

// Exporting the defined colors and sizes for use in other parts of the application
export { colors, sizes };
